import { expect } from 'chai';

/*import { API } from '../../../web/static/js/api';*/

describe('The API', function() {
    it('should get the schedule app', (done) => {
        /*let schedule = API.ScheduleModel.getSchedule();*/

        expect(true).to.be.true;

        done();
    });
});
